# Скопируйте в .env для локальной проверки формы и аналитики. Файл .env не попадает в git.
# В продакшене значения задаются переменными репозитория на GitHub (см. .github/workflows/deploy.yml).
PUBLIC_WEB3FORMS_KEY=
PUBLIC_UMAMI_WEBSITE_ID=
